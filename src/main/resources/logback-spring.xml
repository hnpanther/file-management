<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Sourced from the Spring environment so it can be set per deployment
         (filemanagement.log.path / FILEMANAGEMENT_LOG_PATH) instead of being a path baked
         into the build. -->
    <springProperty scope="context" name="LOG_PATH" source="filemanagement.log.path" defaultValue="./logs"/>
    <property name="LOG_FILE_NAME" value="app_log"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <!-- An <encoder>, not a <layout>: a ConsoleAppender no longer accepts a layout as a
             sub-component, and the nested <charset> was being ignored. -->
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="LogFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE_NAME}.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>

        <!-- SizeAndTimeBasedRollingPolicy, not the deprecated SizeAndTimeBasedFNATP nested inside
             a TimeBasedRollingPolicy. In that older shape maxHistory sat where logback ignores it,
             so archived logs were never deleted and the disk filled up unbounded.
             Rolls daily, and sooner if the file passes maxFileSize. -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archived/${LOG_FILE_NAME}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>10</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <root level="info">
        <appender-ref ref="LogFile"/>
        <appender-ref ref="CONSOLE"/>
    </root>

    <logger name="com.hnp.filemanagement" level="debug"/>

</configuration>
